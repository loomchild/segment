Snapshot:
maven deploy

Release:
maven release:clean
maven -Dusername=jarekl release:prepare
maven release:perform

On error:
Change back to snapshot

Delete tag:
git tag -d <name>
git push origin :refs/tags/<name>

