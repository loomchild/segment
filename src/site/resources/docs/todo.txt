replace strings in doc
bindist


Compare splitting with Marcin Milkowski, add Poleng scripts, compare:

Hej,

Jarek Lipski pisze:
> Hej,
>
> Powoli otrzasam sie po konferencji i wracam do rzeczywistosci. Moze czas na male podsumowanie..
>
> Moim zdaniem spotkanie bylo bardzo owocne na wielu plaszczyznach, prezentacja mimo mojego spoznienia wyszla niezle i wzbudzila duze zainteresowanie.

Racja :)

> Skontaktuje sie z Polengiem w sprawie uzywania przez nich segmenta i ich regul. Jak mozna przetestowac precision / recall ich regul zeby zobaczyc czy w ogole warto, masz moze jakies narzedzie do tego? Moze przeslac ci ten plik?

Mam dwa skrypty w awk. Są dosyć toporne. Po prostu usuwam znaczniki końca zdań z oryginału (konwertuję je wcześniej na <sentence> i </sentence>, dzielę segmentem używając bodajże znaczników <sentence> i </sentence>, tokenizuję oba pliki i porównuję diffem.

Tokenizacja:

-- 
BEGIN {FS="[ <>]"}
{for (i=1; i<=NF; i++)
    print $i
    }

-- 

Porównanie:

-- 

BEGIN {
truefile="frek-tok.txt"
while ((getline < truefile)  > 0){
    if ($0~/\/sentence$/)
        brks++       
    }   
print "True positives in original: " brks
}
/^-\/sentence/ {fneg++}
/^+\/sentence/ {fpos++}
END {
print "Precision: " brks / (brks + fpos)
print "Recall: " brks / (brks + fneg)
print "Accuracy: " brks / (brks + fpos +fneg)
}

-- 

Plik truefile zawiera stokenizowany oryginał z końcami wierszy. Powinienem był utworzyć jakiś skrypt w bashu, żeby to robić automagicznie, ale mi się nie chciało, bo myślałem, że to tylko do jednego artykułu.

> Rozmawiala ze mna pewna Bulgarka na konferencji ze mialaby do zoferowania slownik skrotow w jezyku bulgarskim i pomocy w stworzeniu regul podzialu dla niego. Co o tym myslisz, mamy juz taka obsluge?

Nie mamy, bo LT nie ma bułgarskiego.

> Co myslisz o wyodrebnieniu pliku SRX-a poza LanguageTool, jako ze temat/ jezyki moga wykraczac poza niego? Rozumiem ze to duzo administracyjnej pracy, ale moze warto sie zastanowic?

Czy ja wiem? Dużo roboty z jednym plikiem? ;)

Najłatwiej byłoby postawić w miejscu publicznym link do CVS i już. Plik może zawierać dodatkowe języki, nic to przecież nie zaszkodzi. Inna rzecz, że nie będą bezpośrednio używane.

> Dziekuje za wszystko i mam nadzieje na dalsza wspolprace:)

To ja dziękuję - samemu by mi się nie chciało bawić w implementację standardu...

Coś wspominałeś o rozwalonym thinkpadzie i pozostałych kościach sdram? Chętnie odkupię :)


Write automatic test with real data. It can measure performance and accuracy 
in real-life uses. Problem: Need to obtain SRX and text.

Maybe expand TextIterator interface by adding next(Writer) method. It can save
memory. Implementation can read next fragments in loop instead of doing this 
just once.


WON'T DO ----------------------------------------------------------------------

Maybe add initialize method to TextIterator interface. But then object is not 
fully initialized after construction. Probably then also would need two methods:
initialize() and initialize(parameterMap).

